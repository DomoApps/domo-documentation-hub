---
title: "Create and Use Data Models"
description: "Define relationships between DataSets once and reuse them across Domo for consistent metrics and faster analysis."
---

## Intro

This article explains how to create a Data Model, add DataSets to the canvas, define relationships between DataSets, configure schema metadata, and use a Data Model as a data source in Analyzer.

---

## Required Grants

To create and manage Data Models, you must have an **Editor** or **Admin** role, or a custom role with access to data tools. The DataSets you want to relate must already be available in your Domo instance.

## Create a Data Model

1. From the navigation menu, go to **Features** and select **Data Models** under **Semantic Layer**.

   <Frame>![Data Models option under Semantic Layer in the Features navigation menu](/images/kb/data-models-1.png)</Frame>

2. Select **Create Model** in the top menu. A new model is created and you are taken to the Data Model Editor canvas.

<Frame>![Create Model Action](/images/kb/data-models-2.png)</Frame>

3. Enter a **Name** and an optional **Description** for your model.

  <Frame>![Data Model Editor Canvas](/images/kb/data-models-3.png)</Frame>

<Tip>**Tip:** You can also duplicate an existing model instead of starting from scratch. Open any model and select the duplicate option from the model menu to create a new version you can modify independently.</Tip>


## Add DataSets to the Canvas

The Data Model Editor is a visual canvas where you add DataSets and define the relationships between them.

1. In the Editor, select **Add DataSet**.

  <Frame>![Data Model Editor Canvas](/images/kb/data-models-4.png)</Frame>

2. Search for and select the DataSet you want to include. You can add tables from the following sources:

   - **Domo DataSets —** any DataSet already in your Domo instance.
   - **Data Model outputs —** tables that are outputs of other Data Models.
   - **Snowflake tables —** tables connected directly from Snowflake.

   Click on the **+** action, next to the dataset name to add it to the canvas.

   <Frame>![Add dataset icon](/images/kb/data-models-5.png)</Frame>

   You can also select a dataset by clicking on its name. A panel with the dataset attributes will appear. Click the ** +Add Dataset** button to add it to the canvas.
    
    <Frame>![Add dataset icon](/images/kb/data-models-6.png)</Frame>

3. The DataSet appears as a card on the canvas, showing all of its available columns.

    <Frame>![Add dataset icon](/images/kb/data-models-7.png)</Frame>

4. Repeat to add any additional DataSets that should be part of the model.

<Frame>
  <img
    src="/images/kb/data-model-8.png"
    alt="Data Model Editor canvas showing multiple DataSet cards"
  />
</Frame>

<Tip>**Tip:** For large Data Models with many tables, you can pan and navigate around the canvas freely. Drag DataSet cards to rearrange them and keep the layout easy to read. 
Use the zoom out,zoom in, center canvas and mini-map controls to easily navigate through your model</Tip>

<Frame>![Add dataset icon](/images/kb/data-models-9.png)</Frame>

## Define Relationships Between DataSets

After your DataSets are on the canvas, connect them by dragging from a key column in one DataSet to the matching key column in another.

1. Hover over the join key column in one DataSet.

2. Drag from that column to the corresponding key column in the related DataSet.

   A relationship line appears connecting the two tables.

<video
  autoPlay
  muted
  loop
  playsInline
  className="w-full aspect-video rounded-xl"
  src="/images/kb/data-models-10.mp4"
></video>

3. Select the relationship line to review or edit its settings:

   - **Relationship Keys —** the columns on each side of the join (for example, `Account UID` on the Accounts table joined to `Account UID` on the Orders table).
   - **Cardinality —** the nature of the relationship between the two tables:
     - **One-to-Many —** one record in the first table relates to many records in the second (for example, one Account can have many Orders).
     - **Many-to-One —** many records in the first table relate to one record in the second.
     - **One-to-One —** each record in the first table relates to exactly one record in the second.

<video
  autoPlay
  muted
  loop
  playsInline
  className="w-full aspect-video rounded-xl"
  src="/images/kb/data-models-11.mp4"
></video>

<Note>**Note:** You can relate DataSets from disparate source systems through shared keys in a single model. There is no need for a separate DataFlow per combination.</Note>

4. Preview table data and search for columns by selecting a table in the canvas.

<video
  autoPlay
  muted
  loop
  playsInline
  className="w-full aspect-video rounded-xl"
  src="/images/kb/data-models-12.mp4"
></video>

5. Select **Save Model** to preserve your relationship configuration. The following actions are available: 
  - ** Save Model -** saves your configuration without exiting the Editor canvas.
  - ** Save and Close -** saves your configuration and exit the Editor canvas. 
  - ** Save As -** Makes a copy of your model.

<Frame>
  <img
    src="/images/kb/data-models-13.png"
    alt="Save Settings"
  />
</Frame>

## Delete Relationships and Objects
To delete all relationships for a table:
1. Click on the three-dotted menu, in the top-right corner of an table. Select the **Break all relationshios** option.
<Frame>
  <img
    src="/images/kb/data-models-14.png"
    alt="Save Settings"
  />
</Frame>
2. Click **Delete** to confirm you want to delete all attached relationships. Choose **Cancel** to cancel the operation. 

To delete a specific relationship: 
1. Select the table. 
2. Navigate to the **Releationships** tab, in the preview modal.
3. Choose the relationship you want to delete. 
4. Click the **Delete Relationship** action.
5. Click **Delete** to confirm you want to delete all attached relationships. Choose **Cancel** to cancel the operation. 

<Frame>
  <img
    src="/images/kb/data-models-15.png"
    alt="Save Settings"
  />
</Frame>


## View the Data Model Landing Page

Each Data Model has a landing page. The landing page displays the model's name, description, and model's attributes such as author,last updated timestamp.

<Frame>
  <img
    src="/images/kb/data-models-17.png"
    alt="Save Settings"
  />
</Frame>

To make changes to the model, select **Edit Data Model** from the top menu. This takes you to the Data Model Editor canvas.
To perform at-hoc data exploration in a Worksheet, select **Explore** 
To use your data model in Analyzer, select **Open With** and choose *Analyzer**
To create a visualization card based on your model, select ** Create Visualization ** 
To create an App Studio App, select **Create App**
To share your data model with other users, select ** Share This Dataset** 
To add your data model to a Workspace, select ** Add to Workspace** 

This actions are also available in the three-dotted menu on the top-right corner of the Landing Page.

<Frame>
  <img
    src="/images/kb/data-models-16.png"
    alt="Save Settings"
  />
</Frame>


## Configure the Schema Tab

The Schema tab lets you enrich each field in your Data Model with metadata. This helps downstream users understand the data and improves the accuracy of AI-assisted features.

1. Open your Data Model and select the **Schema** tab.
<Frame>
  <img
    src="/images/kb/data-models-18.png"
    alt="Schema Tab"
  />
</Frame>

2. For each dataset and column, you can set the following:

   - **Reorder —** control how columns appear in the dataset schema.
   - **Description —** document what the field represents.
   - **More Options —**
   <Frame>
  <img
    src="/images/kb/data-models-19.png"
    alt="Schema Tab"
  />
</Frame>
   - **Tags —** help users search and discover fields.
   - **AI Enabled —** enable columns to be discovered by DOMO AI.
   - Add **AI Synonyms** and **AI Context** to improve how Domo AI interprets natural language queries against this model.
   
 <Frame>
  <img
    src="/images/kb/data-models-20.png"
    alt="Schema Tab"
  />
</Frame>

3. Select **Save** after making your changes.

<Tip>**Tip:** Well-documented fields in the Schema tab significantly improve the accuracy of answers from Domo AI Chat when users ask questions about data connected through this model.</Tip>


## Use a Data Model in Analyzer

After a Data Model is created, it is available as a data source in Analyzer—just like any standard DataSet. Columns from all related tables appear together with no additional joining required.

1. Navigate to the dashboard where you want to build a new card.

2. Select **New Card** and select **Visualization**.

3. In the DataSet picker, search for your Data Model by name and select it.

4. Build your card normally. All columns from the model's related DataSets are available in a single view.

<Note>**Note:** Because joins happen at query time, card performance may vary depending on the size of the underlying DataSets and the complexity of the relationships in the model.</Note>


## Troubleshoot

<AccordionGroup>

<Accordion title="My card in Analyzer is returning unexpected or duplicate rows.">
  Review the cardinality settings on your relationship lines in the Editor. A one-to-many or many-to-many relationship can produce multiple rows if the join key is not unique. Verify that the correct key columns are selected on both sides of the relationship.
</Accordion>

<Accordion title="Schema tab changes are not showing in existing cards.">
  Schema metadata updates—descriptions, tags, and AI fields—may require a card refresh or re-open to reflect. The underlying data is not affected by Schema tab changes.
</Accordion>

<Accordion title="I can't find my Snowflake table when adding DataSets.">
  Confirm that your Snowflake Cloud Integration is connected and authenticated in your Domo instance. Snowflake tables are available in the DataSet picker only after a successful connection is established.
</Accordion>

</AccordionGroup>

## Related Articles

- [Creating and Running DataFlows in Magic ETL](/s/article/Creating-and-Running-DataFlows-in-Magic-ETL)
- [Using Beast Mode Calculations](/s/article/Using-Beast-Mode-Calculations)
- [Building Cards in Analyzer](/s/article/Building-Cards-in-Analyzer)
- [Configuring Cloud Integrations (Snowflake)](/s/article/Configuring-Cloud-Integrations-Snowflake)
- [Configuring Domo AI Chat for Your Instance](/s/article/Configuring-Domo-AI-Chat-for-Your-Instance)
- [Domopalooza Release Notes](/s/article/000004968)
