# Phase 3b RE-ROUTE cluster: reroute__4-embed-office

These gaps were correctly skipped by earlier waves because the gap data mislinked them. Each is re-homed to its correct article below. Some target files were already edited in an earlier wave — read current state first and add ONLY the new gap's content.

Follow the shared agent instructions (3 quality gates, [pm-input] deferral, no TODO markers, imperative Title Case, English-only, no Next Steps/Related links).

---

## `s/article/360042932994.mdx`
**Marker PM for this file:** Chris Wright

### Gap rank 353 (Low, score 37.5) — Deep links / shareable filtered dashboard views
- **Routing note:** Sharing and Removing Access to Cards and Dashboards (was pointed at the embed-only filter reference 4418999855639). Add the in-app deep-link how-to: pfilter URL construction + object-ID-based page/card linking (distinct from embed-only pfilters).
- **What's missing:** Feature request, but an adjacent documentable how-to: pfilter deep-link URL construction and object-ID-based card/page linking patterns (mycompany.domo.com/page/.../kpis/details/...) referenced in comments.
- **Original suggested location:** Update or add to a dashboard-sharing article (e.g., s/article/360042932994.mdx) an interim how-to on constructing a filtered-page deep link via pfilter URL parameters and object-ID-based page/card linking for in-app use, distinct from the embed-only pfilter docs. Note auto-generating such links from the UI is a feature request.

---

## `s/article/000005143.mdx`
**Marker PM for this file:** Khushboo

### Gap rank 121 (Medium, score 58.9) — Office Add-In feature gaps vs legacy and PowerPoint export behavior
- **Routing note:** Office Add-In User Guide (was pointed at the install-only guide 000005146). Add/confirm add-in usage behavior: PowerPoint one-card-per-slide, slide layout/ordering, image resolution, 100-row import preview. Much may already be covered — read first and add only genuinely new items; defer relative-date-filtering roadmap / Excel re-import-in-place / Google Slides via [pm-input].
- **What's missing:** Document the new Office/PowerPoint Add-In capabilities and limitations vs the legacy plug-in: one-card-per-slide behavior, slide layout/template/ordering, image resolution, dataset import preview/paging, relative date filtering (roadmap), and the Excel re-import-in-place limitation. Several items (relative date filtering, re-import in place, Google Slides) are feature requests.
- **Original suggested location:** Update the Office Add-ins documentation (s/article/000005146.mdx or a companion how-to) with a capabilities/limitations section vs the legacy plug-in: PowerPoint one-card-per-slide and slide layout/ordering, image resolution, dataset import preview behavior, relative date filtering (roadmap), Excel re-import limitation. Note Google Slides has no integration (feature request).

---
