export const LegacyNote = ({ replacement, replacementLabel = "the recommended approach" }) => (
  <Warning>
    <strong>Legacy:</strong>{" "}
    This feature is no longer actively maintained and is kept available for backwards compatibility only.{" "}
    {replacement
      ? <>For the current recommended approach, see <a href={replacement}>{replacementLabel}</a>.</>
      : "For the current recommended approach, see your Domo account team."
    }
  </Warning>
);
