export const SunsetNote = ({ date, replacement, replacementLabel = "the recommended replacement" }) => (
  <Warning>
    <strong>Sunset:</strong>{" "}
    {date
      ? <>This feature has reached end of life and will be shut down on <strong>{date}</strong>. Support and updates have ended.</>
      : "This feature has reached end of life. Support and updates have ended."
    }{" "}
    {replacement
      ? <>Please migrate to <a href={replacement}>{replacementLabel}</a>{date ? " before that date" : ""} to avoid disruption.</>
      : "Please migrate to the recommended replacement to avoid disruption."
    }
  </Warning>
);
